package com.smartcampost.backend.model.enums;

/**
 * Processing status for system events.
 */
public enum EventProcessingStatus {
    PENDING,
    PROCESSING,
    COMPLETED,
    FAILED,
    SKIPPED
}
